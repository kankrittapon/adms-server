# ADMS Server Project Status

## Active System Baseline

* **Target Biometric Terminal**: SONIC / ZKTeco ZEM560_TFT (MIPS Linux 2.6.24 Treckle, Firmware `Ver 6.60 Aug 26 2011`)
* **Host Address**: `192.168.1.248` (AI-Brain Host) / Terminal: `192.168.1.201`
* **Collector Protocol**: ZK Binary Protocol over TCP 4370 (`pyzk==0.9`, Comm Key `600`)
* **Collector Engine**: Modular Finite State Machine (`app/collector.py`)
* **Backfill System**: Hybrid Reconciliation (`BACKFILLING` -> `LIVE`)
* **Healthcheck System**: Atomic Ephemeral State File (`/tmp/collector_health.json`) + Non-Invasive CLI (`app/healthcheck.py`)
* **Identity Architecture**: Human Master (`human_employees`) & Device Identity (`device_users`) Strict Separation
* **SQL Identity Foundation**: Additive Migration `sql/002_identity_foundation.sql`, Constraint Migration `sql/003_legacy_identity_constraint.sql` & Provenance Schema `sql/004_human_master_schema.sql` Applied
* **Collector Ingestion Pipeline**: Upgraded to Device Identity First (`ensure_device_user()`). Legacy stub creation (`ensure_employee_stub()`) **REMOVED**.
* **Documentation Architecture**: Categorized into 6 domain directories (`docs/architecture/`, `docs/device/`, `docs/collector/`, `docs/data/`, `docs/database/`, `docs/operations/`, `docs/external/ai-brain/`).

---

## Locked Execution Sequence

The project SHALL follow this order:

1. **`ADMS-Docs-Categorize-002`**: Documentation reorganization (**COMPLETE**).
2. **`ADMS-Data-ExcelImport-002`**: Human Master Excel Import (**COMPLETE**).
3. **`ADMS-Checkpoint-PostExcelImport-001`**: Post-import checkpoint (**COMPLETE**).
4. **`ADMS-Data-HumanDeviceMapping-001`**: Human ↔ Device Mapping Workflow Plan (**COMPLETE**).
5. **`ADMS-Data-HumanDeviceMappingSchema-001`**: Human ↔ Device Mapping Schema Enhancement (**COMPLETE** — Designed DDL migration `sql/005_human_device_mapping_schema.sql`).
6. **`ADMS-Data-DeviceUserLifecycle-001`**: Device User Lifecycle / Account Incarnation Audit (**COMPLETE** — Verified `ensure_device_user()` identity reuse, recycling risks, selected Decision B).
7. **`ADMS-Data-HumanDeviceMappingSchema-002`**: Human ↔ Device Mapping Schema Migration Execution (**COMPLETE** — Created migration DDL `sql/005_human_device_mapping_schema.sql`).
8. **`ADMS-Checkpoint-PostMappingSchema-001`**: Post Mapping Schema Recovery Checkpoint (**COMPLETE** — Resolved backup metadata conflict, verified 33/33 tests pass, locked next phase).
9. **`ADMS-Server-DatabaseRebuild-001`**: Server Database Rebuild (**COMPLETE** — 3 Docker fixes, Collector operational, 7 attendance backfilled, authoritative backup verified).
10. **`ADMS-Checkpoint-PostServerRebuild-001`**: Post-Rebuild Checkpoint (**COMPLETE** — READ-ONLY validation PASS, all 3 nodes synced at `a240061`).
11. **`ADMS-Collector-TemporalIdentity-001`**: Collector Temporal Identity Audit & Plan (**COMPLETE — PLAN ONLY** — Audited current timeless resolver, designed temporal `[valid_from, valid_to)` contract, identified TIMEZONE BLOCKER).
12. **`ADMS-Collector-TimestampTimezone-001`**: Timestamp Timezone Audit & Plan (**COMPLETE — PLAN ONLY** — Verified +7h offset on all 7 rows, designed `normalize_device_timestamp()` with `ZoneInfo("Asia/Bangkok")`, deterministic historical correction strategy, 17 planned tests).
13. **`ADMS-Collector-TimestampTimezone-002`**: Timestamp Timezone Implementation (**COMPLETE** — Implemented `normalize_device_timestamp()` with `ZoneInfo("Asia/Bangkok")`, corrected 7 existing rows -7h, rebuilt collector, verified runtime deduplication, 54/54 tests pass).
14. **`ADMS-Collector-TimestampTimezone-003`**: Timestamp Timezone Live Verification Checkpoint (**COMPLETE** — READ-ONLY checkpoint PASS, all 7 rows round-trip verified, 0 duplicates, 54/54 tests pass, backups verified, TemporalIdentity-002 UNBLOCKED).
15. **`ADMS-Collector-TemporalIdentity-002`**: Temporal Identity Implementation (**COMPLETE** — Implemented temporal `resolve_verified_employee_mapping(cur, device_user_pk, scan_time)` with `[valid_from, valid_to)` semantics, VERIFIED-only, LIMIT 2 ambiguity defense, realtime + backfill integration, 33 new tests (87/87 total pass), deployed to ai-brain, collector rebuilt, runtime LIVE+HEALTHY, 0 mappings, all attendance employee_id NULL).
16. **`ADMS-Collector-TemporalIdentity-003`**: Temporal Identity Live Verification Checkpoint (**COMPLETE** — READ-ONLY checkpoint PASS, resolver source verified on ai-brain, VERIFIED-only + `[valid_from, valid_to)` + ambiguity fail-safe confirmed, realtime + backfill paths verified, 87/87 tests pass, 0 mappings, runtime LIVE+HEALTHY, backup verified, Human ↔ Device Mapping PLAN AUTHORIZED).
17. **`ADMS-Collector-AttendanceParseTime-001`**: Parse_Time Bug Fix (**RESOLVED** — implemented inside `ADMS-Backend-Finalization-001`: `parse_time()` now accepts `HH:MM` and `HH:MM:SS`; all 10 historical UNKNOWN rows deterministically corrected; 224/224 tests).
18. **`ADMS-Data-HumanDeviceMapping-002`**: Human ↔ Device Mapping Workflow Plan (**COMPLETE — READ-ONLY / WORKFLOW PLAN ONLY** — Designed administrator-controlled mapping workflow, evidence hierarchy, controlled scan workflow, temporal `[valid_from, valid_to)` policy, audit metadata vocabulary, overlap protection, reconciliation design, candidate reports for device users 1 and 2. Both device users have generic placeholder display names — controlled scan is the ONLY viable verification path. 0 mappings created.).
19. **`ADMS-Data-DeviceEnrollmentWorkflow-001`**: Device Enrollment Workflow Post-Reset Audit & Plan (**COMPLETE — READ-ONLY LIVE AUDIT / PLAN ONLY + DOCUMENTATION WRITE ONLY** — Owner manually deleted all terminal users. Verified: 0 terminal users, 7 attendance preserved in flash, DB 120/2/0/7. Confirmed lifecycle gap. Designed production enrollment workflow: ID namespace 1001+, 12-step controlled enrollment, account state machine, pilot design, bulk rollout. 0 terminal writes, 0 mappings.).
20. **`ADMS-Data-DeviceUserLifecycle-002`**: Device User Lifecycle Implementation (**COMPLETE** — Implemented `reconcile_roster_lifecycle()` in `app/db.py`, `perform_roster_lifecycle_check()` in `app/collector.py`, `roster_poll_interval_seconds` in `app/config.py`, 18 new tests (105/105 total pass), deployed to ai-brain, first poll LIVE VERIFIED: 0 terminal users → both device_users marked inactive_at, 0 deletions, 0 mappings, 0 schema changes, PostgreSQL/MQTT untouched).
21. **`ADMS-Data-DeviceUserLifecycle-003`**: Device User Lifecycle Live Verification Checkpoint (**COMPLETE** — READ-ONLY checkpoint PASS. All 11 critical properties verified. `inactive_at` stable across 13+ periodic polls (T1==T2). 105/105 tests pass. 0 restarts, 0 terminal writes, 0 DB exceptions, 0 UID anomalies. Human Master 120/120 preserved. Backups verified. DeviceEnrollmentWorkflow-002 UNBLOCKED.).
22. **`ADMS-Data-DeviceEnrollmentWorkflow-002`**: Device Enrollment Workflow Implementation (**COMPLETE** — Implemented `app/enrollment.py` (ID allocator 1001+, reservation, safe `set_user()` account creation, 9-state workflow, controlled scan), applied migration `sql/006` with verified pre/post backups, 63 new tests (168/168 pass), deployed to ai-brain (listener rebuilt only), pilot infrastructure READY, 0 terminal accounts created, 0 mappings. Next: One-Human Pilot).
23. **`ADMS-Data-DeviceEnrollmentPilot-001`**: One-Human Controlled Pilot (**COMPLETE — PASS, READY_FOR_MAPPING reached** — Owner confirmed pilot Human กฤตพล หมาดเส็น (พ.จ.ต.); reserved production ID 1001; created exactly ONE terminal account `cpo3 Krittapon M` (NORMAL privilege, device_uid 1, pyzk False-return quirk resolved via owner-approved reconciliation); physical fingerprint enrollment by Human; ONE controlled scan verified (attendance id 12, 2026-08-12 08:47:37+00, unambiguous, employee_id NULL); owner confirmed identity at both gates; enrollment #1 READY_FOR_MAPPING. 0 mappings, 0 bulk enrollment, 168/168 tests, runtime HEALTHY, pre/post backups verified.).
24. **`ADMS-Data-HumanDeviceMapping-003`**: First VERIFIED Mapping + RTN Rank Normalization (**COMPLETE** — Created the first production VERIFIED temporal mapping from pilot evidence: employee `039c4486-b30f-4ce1-b780-783cd268858d` ↔ device_user_pk 7 (terminal 1001), valid_from `2026-08-12 08:47:37+00` (CONTROLLED_SCAN), valid_to NULL, verified_by owner-krittaphol, mapping_id 1. Resolver verified live (before→None, at/after→Human, legacy→None). Implemented RTN rank normalization reference layer `app/rtn_ranks.py` + import-boundary พลทหาร exclusion (`--exclude-plothan`); audited 36 existing พลทหาร records — NOT deleted, archival migration proposed. 211/211 tests, runtime HEALTHY, pre/post backups verified.).
25. **`ADMS-Data-HumanDeviceMapping-004`**: Post-Mapping Checkpoint + Owner Gate (**COMPLETE — CHECKPOINT PASS** — Independently verified git/runtime/DB/mapping id 1/resolver/enrollment chain/attendance/rank normalization/พลทหาร/backup. Owner selected **C (Both, sequentially)** at the submit gate → sub-phases executed in the SAME session: `ADMS-Data-AttendanceReconciliation-001` (attendance id 12 → employee `039c4486…`, 1 row, legacy untouched, raw_payload unchanged) and `ADMS-Data-PlothanProductionExclusion-001` (migration 007 `production_scope` flag; 36 พลทหาร excluded reversibly, 0 collateral; enforcement in reservation; 213/213 tests; runtime HEALTHY).).
26. **`ADMS-Data-MultiFingerprintValidation-001`**: Same-Human Multi-Fingerprint Validation (**COMPLETE — PASS** — Owner physically enrolled a SECOND fingerprint on the ZEM560 terminal under EXISTING User 1001; roster re-read confirmed User 1001 intact (`1001 / uid 1 / privilege 0`) and NO User 1002 created. Fingerprint slot count NOT SAFELY OBSERVABLE (no template retrieval) → behavioral validation. Two controlled second-finger scans (attendance id 15 @ 13:28:04+00 and id 16 @ 13:30:47+00, both in the 5-min window) — both resolved AUTOMATICALLY through mapping_id 1 to the SAME Human `039c4486…` (REALTIME_RESOLUTION PASS; no manual UPDATE). Owner confirmed both scans. Mapping count remained exactly 1, device_user_pk 7, valid_to NULL, DUPES=0, legacy 7 rows untouched, 36 พลทหาร still excluded, pilot Human production_scope=true. 213/213 tests, runtime HEALTHY (restarts 0, HC_RC=0), post-validation backup verified (`adms_post_multifinger_20260812_133403.dump`, 55,013 B, SHA256 `69bd1be8…`, 105 TOC). Proves ONE HUMAN → ONE ACCOUNT → MULTIPLE FINGERPRINTS → ONE VERIFIED TEMPORAL IDENTITY. Does NOT validate multi-person enrollment.).
27. **`ADMS-Backend-Finalization-001`**: Backend/Data/Identity Finalization (**COMPLETE — BACKEND FOUNDATION 100%** — parse_time defect fixed (`HH:MM:SS` support), 10/10 historical UNKNOWN attendance rows deterministically corrected to 8 LATE + 2 ON_TIME (status-only, production window 05:00:00–10:00:00), 11 new tests (224/224 PASS), listener rebuilt on ai-brain (restarts 0, HC_RC=0), full regression verified (identity/multi-fingerprint/production-scope/RTN ranks/dedupe/backfill all PASS), pre-backup `adms_pre_finalization_20260812_140255.dump` + **authoritative final backup `adms_backend_final_20260812_140826.dump`** (55,149 B, SHA256 `08169e66…`, 105 TOC, pg_restore -l PASS). Backend completeness A–Z all COMPLETE. Frontend development UNBLOCKED (API gap noted). Canonical baseline documented in `docs/BACKEND_PRODUCTION_BASELINE.md`. Native ADMS Push remains EXPERIMENTAL/DEFERRED.).
28. **`ADMS-Frontend-Plan-001`**: Frontend Architecture & UX Plan (**COMPLETE — PLAN ONLY** — Owner selected option A at the backend-finalization gate. Produced `docs/FRONTEND_ARCHITECTURE_PLAN.md`: stack proposal (FastAPI API layer + React/TS/Vite frontend — approval required before code), information architecture/navigation (Dashboard, Personnel, Devices, Attendance, Enrollment, Mapping, System), permissions model (viewer/operator/admin), enrollment operator workflow UX (evidence-gated, one-at-a-time), attendance/identity views, API contract sketch for the confirmed **Frontend API Gap** (no UI-facing HTTP layer exists; read-only + gated write endpoints proposed), and owner-gated phases F1–F5. No frontend code written.).
29. **Native ADMS Push E2E**: EXPERIMENTAL TRACK ONLY (Isolated verification after identity workflow foundation is complete).

---

## Current Identity & Checkpoint State

* **Collector Identity Transition**: COMPLETE (`ADMS-Collector-IdentityTransition-002`)
* **Device Identity Model**: OPERATIONAL (`devices` PK 1, `device_users` 2 accounts)
* **Human Master Auto-Creation**: DISABLED (Collector NEVER auto-creates Human rows)
* **Legacy Stub Creation**: DISABLED (0 new stubs created)
* **Unmapped Attendance**: SUPPORTED (`employee_id = NULL` persisted cleanly)
* **Human Master Schema Foundation**: COMPLETE (`sql/004_human_master_schema.sql`: `branch`, `category`, `human_employee_sources` applied)
* **Documentation Categorization**: COMPLETE (`ADMS-Docs-Categorize-002`: 20 canonical docs moved to category directories, relative Markdown links active)
* **Human Master Excel Data Import**: COMPLETE (`human_employees` 120 records, `human_employee_sources` 120 records, dry-run verified 0 NEW / 120 UNCHANGED)
* **Human ↔ Device Mapping Plan**: COMPLETE (`ADMS-Data-HumanDeviceMapping-001` — PLAN ONLY)
* **Human ↔ Device Mapping Schema Plan**: COMPLETE (`ADMS-Data-HumanDeviceMappingSchema-001` — PLAN ONLY)
* **Device User Lifecycle Audit**: COMPLETE (`ADMS-Data-DeviceUserLifecycle-001` — PLAN ONLY)
* **device_user recycling**: VERIFIED RISK (Reuses `device_user_pk` on conflict `(device_id, device_user_id)`)
* **current account incarnation support**: PARTIAL / MODIFIED FOR MIGRATION 005 (Decision B selected)
* **Human ↔ Device Mapping Schema Migration**: COMPLETE (`sql/005_human_device_mapping_schema.sql` created incorporating `roster_last_seen_at`, `inactive_at`, `valid_from`, `valid_to`, `verified_by`, `verification_method`, `verification_note`)
* **Device User Lifecycle Storage**: IMPLEMENTED (`roster_last_seen_at`, `inactive_at` in migration DDL)
* **Automatic Roster Lifecycle Detection**: IMPLEMENTED & LIVE VERIFIED (`ADMS-Data-DeviceUserLifecycle-002` — `reconcile_roster_lifecycle()` + `perform_roster_lifecycle_check()`, periodic polling every 300s, both device_users marked inactive_at after successful empty roster, 0 deletions)
* **Controlled Enrollment Infrastructure**: IMPLEMENTED & DEPLOYED (`ADMS-Data-DeviceEnrollmentWorkflow-002` — `app/enrollment.py`: ID allocator 1001+, reservation, safe `set_user()` account creation, 9-state workflow, controlled scan; migration `sql/006` applied on ai-brain; pilot infrastructure READY)
* **One-Human Controlled Pilot**: COMPLETE — PASS (`ADMS-Data-DeviceEnrollmentPilot-001` — production terminal account 1001 `cpo3 Krittapon M` (NORMAL, device_uid 1) created; fingerprint physically enrolled; controlled scan verified (attendance id 12, 2026-08-12 08:47:37+00); enrollment #1 READY_FOR_MAPPING; pre/post pilot backups verified; 168/168 tests; runtime HEALTHY; 0 mappings created)
* **Human ↔ Device VERIFIED Mappings**: 1 record (mapping_id 1: employee `039c4486-b30f-4ce1-b780-783cd268858d` ↔ device_user_pk 7, valid_from `2026-08-12 08:47:37+00`, valid_to NULL, CONTROLLED_SCAN)
* **Human ↔ Device Mapping WRITE**: LIMITED AUTHORIZED (exactly 1 VERIFIED mapping created from pilot evidence; bulk/automatic mapping still PROHIBITED)
* **Automatic Sequential user_id Mapping**: PROHIBITED (Excel row 1 != ZKTeco user_id 1)
* **ZKTeco Terminal Writes from Human Import**: NONE (0 terminal socket calls)
* **Remote Fingerprint Enrollment**: UNSUPPORTED / NOT USED
* **RTN Rank Normalization**: IMPLEMENTED (`ADMS-Data-HumanDeviceMapping-003` — `app/rtn_ranks.py` canonical catalog: 9 officers, 6 NCO, 1 enlisted + ว่าที่ acting forms; reference-layer only, no schema change; original rank text preserved; rank is metadata, never identity)
* **พลทหาร Exclusion Policy**: IMPLEMENTED + ENFORCED (`ADMS-Data-PlothanProductionExclusion-001` — migration 007 `production_scope` flag: **36 พลทหาร records flagged `production_scope=false`** (reversible, 0 deletion, UUIDs/provenance preserved); deterministic `is_plothan()` + import boundary `--exclude-plothan`; `reserve_next_device_user_id()` requires `production_scope=true`)
* **Native ADMS Push E2E**: NOT STARTED (EXPERIMENTAL / DEFERRED)
* **Real PostgreSQL Post-Import Recovery Backup**: VERIFIED (`adms_post_excel_import_20260811_121449.dump`, SHA256 `d621f280...`)
* **Backup Format**: `pg_dump` Custom Format (PGDMP_V1)
* **Database Rebuild**: COMPLETE (`ADMS-Server-DatabaseRebuild-001` — database reconstructed from canonical sources: migrations 001-005, Excel Human Master 120 records, live ZKTeco terminal 7 attendance records, 2 device users)
* **Collector Docker Package Fix**: COMPLETE (3 fixes: Dockerfile `COPY app/ ./app/` + `CMD python -m app.main`, `iputils-ping`, DB env vars in docker-compose.yml)
* **Authoritative Reconstructed Backup**: VERIFIED (`adms_reconstructed_authoritative_20260811_153725.dump`, 44980 bytes, SHA256 `5386681d...`, `pg_restore -l` VERIFIED 79 TOC entries)
* **Backup Archive Readability**: VERIFIED via `pg_restore -l`
* **Temporal Identity Audit**: COMPLETE (`ADMS-Collector-TemporalIdentity-001` — PLAN ONLY — current resolver is TIMELESS, temporal contract designed `[valid_from, valid_to)`)
* **Timezone Blocker**: RESOLVED (`ADMS-Collector-TimestampTimezone-002` — `normalize_device_timestamp()` with `ZoneInfo("Asia/Bangkok")` implemented in `app/timestamp_utils.py`, applied to realtime/backfill/MQTT/watermark paths, 7 historical rows corrected -7h, runtime deduplication verified, 54/54 tests pass)
* **Parse_Time Bug**: IDENTIFIED (NON-BLOCKING — `parse_time()` fails on `HH:MM:SS`, only affects `status` field, `scan_time` unaffected)
* **Temporal Resolver**: IMPLEMENTED (`ADMS-Collector-TemporalIdentity-002` — `resolve_verified_employee_mapping(cur, device_user_pk, scan_time)` with `[valid_from, valid_to)` semantics, VERIFIED-only, LIMIT 2 ambiguity defense, realtime + backfill integration, 87/87 tests pass, deployed to ai-brain, 0 mappings, all attendance employee_id NULL)
* **Historical Reconciliation**: PLANNED (NOT IMPLEMENTED — future unmapped-only retroactive resolution)
* **Device UID Role**: DIAGNOSTIC ONLY (not canonical identity, not tracked by current ingestion)

---

## Recovery Coordinates

* **SOURCE RECOVERY POINT**: Git Commit `242698f` (`feat: design temporal human-device mapping and lifecycle schema foundation (# PromptID: ADMS-Data-HumanDeviceMappingSchema-002)`)
* **DATABASE RECOVERY POINT**: Archive `adms_post_excel_import_20260811_121449.dump` (7,389 bytes, SHA256 `d621f280af2fc3ebcf7e927afd55486cf5b9009cc1603300cc0d2ac60f9ed00a`)

---

## Completed Tasks & Reports

| PromptID | Date | Type | Status | Summary / Key Deliverables |
| -------- | ---- | ---- | ------ | -------------------------- |
| `ADMS-Bootstrap-ZEM560-001` | 2026-08-11 | Device Baseline | COMPLETE | Established ADMS Server baseline and reconstructed ZEM560 device technical profile. |
| `ADMS-Device-LiveFingerprint-001` | 2026-08-11 | Device Fingerprint | COMPLETE | Live hardware profile: SONIC / ZEM560_TFT (MIPS CPU, Linux 2.6.24, Telnet TCP 23, ZK TCP 4370). |
| `ADMS-Collector-Reliability-001` | 2026-08-11 | Reliability Plan | COMPLETE | Production-grade reliability model, hybrid event capture & backfill architecture, state machine. |
| `ADMS-Device-AttendanceBehavior-001` | 2026-08-11 | Attendance Behavior | COMPLETE | Live device test of `get_attendance()` (0.18s for 6 records), clock drift (-25.39s), `pyzk` 10s socket yields. |
| `ADMS-Device-CapabilityProfile-001` | 2026-08-11 | Capability Spec | COMPLETE | Verified capacity spec (30k users, 3k templates, 100k logs), 5-tier project usability framework. |
| `ADMS-Collector-StateEngine-001` | 2026-08-11 | State Engine Plan | COMPLETE | Modular FSM architecture design, bounded exponential backoff ($2\text{s}\to 60\text{s}$ with $\pm 20\%$ jitter). |
| `ADMS-Data-ExcelProfile-001` | 2026-08-11 | Data Profiling | COMPLETE (Profiling Only) | Analysis and normalization profile for `รายละเอียด กพ.พัน.สอล.ฯ ก.พ.69.xlsx` (120 unique records). **PostgreSQL import NOT performed**. |
| `ADMS-Collector-StateEngine-002` | 2026-08-11 | State Engine Execution | COMPLETE | Refactored `app/main.py` into modular FSM (`app/config.py`, `app/collector.py`, `app/db.py`, `app/mqtt_client.py`). |
| `ADMS-Collector-HybridBackfill-001` | 2026-08-11 | Hybrid Backfill Plan | COMPLETE | Design for historical `get_attendance()` log backfill, client-side watermark filtering, 500-record batch ingestion, MQTT suppression, and 15-minute periodic reconciliation cadence. |
| `ADMS-Collector-HybridBackfill-002` | 2026-08-11 | Hybrid Backfill Execution | COMPLETE | Implemented historical attendance log backfill (`app/collector.py`, `app/db.py`, `app/config.py`), unit test suite & 100k synthetic benchmark (9/9 passed, 0.0040s filtering), live verification against SONIC ZEM560_TFT terminal (6 records backfilled in 0.2008s, MQTT suppressed, 100% idempotent). |
| `ADMS-Checkpoint-CollectorFoundation-001` | 2026-08-11 | Foundation Checkpoint | COMPLETE | Established verified collector foundation baseline after State Engine and Hybrid Backfill implementation. |
| `ADMS-Device-RemoteEnrollmentCapability-001` | 2026-08-11 | Capability Test | COMPLETE | Controlled live test of `enroll_user()`: Command times out without activating on-screen UI on standalone firmware `Ver 6.60`. Classified as **DO NOT USE / NOT RECOMMENDED FOR PRODUCTION**. |
| `ADMS-Device-FirmwareFilesystemAudit-001` | 2026-08-11 | Filesystem Audit | COMPLETE | Read-only Telnet inspection of MTD partitions, `/mnt/mtdblock/options.cfg`, `AttState=0` (default Check-In punch state key, unrelated to remote enrollment), driver nodes (`/dev/tft_lcd`), and generic config vs physical hardware matrix. |
| `ADMS-Device-NativePushVerification-001` | 2026-08-11 | Protocol Verification | COMPLETE | Protocol & socket inspection of native Push config (`AuthServerIP=192.168.1.248:8000`, `libhttppush.so`), embedded HTTP web server (TCP Port 80, `ZK Web Server`), and evaluation. Reconfirmed Python Collector over TCP 4370 as the primary production architecture. |
| `ADMS-Collector-Healthcheck-001` | 2026-08-11 | Healthcheck Plan | COMPLETE | Detailed design for atomic ephemeral health status file (`/tmp/collector_health.json`), state-aware liveness thresholds (LIVE/BACKOFF 120s, BACKFILLING 600s), non-invasive `app/healthcheck.py` CLI module, and Docker Compose parameters. |
| `ADMS-Collector-Healthcheck-002` | 2026-08-11 | Healthcheck Execution | COMPLETE | Implemented atomic health status updates (`app/collector.py`), non-invasive CLI health evaluation module (`app/healthcheck.py`), Docker Compose healthcheck block (`docker-compose.yml`), test suite (22/22 passed), live verification against physical terminal (Exit Code 0 verified during LIVE state). |
| `ADMS-Data-IdentityMapping-001` | 2026-08-11 | Identity Mapping Plan | COMPLETE | Detailed design for strict separation of Human Master Data (`employees`) and Device-Local Identity (`device_users`), multi-device mapping schema (`devices`, `device_users`, `employee_device_mappings`), rejection of Excel row-number mapping assumption, and unmapped attendance ingestion policy. |
| `ADMS-Data-IdentitySchema-001` | 2026-08-11 | Identity Schema Plan | COMPLETE | Detailed DDL migration design (`sql/002_identity_foundation.sql`), additive zero-data-loss architecture (`devices`, `device_users`, `human_employees`, `employee_device_mappings`), seed queries for physical terminal (`3392113170057`), and 5-stage migration path. |
| `ADMS-Checkpoint-PreIdentitySchema-001` | 2026-08-11 | Pre-Schema Checkpoint | COMPLETE | Established clean, verified repository checkpoint baseline prior to executing the first additive database schema migration (`sql/002_identity_foundation.sql`). |
| `ADMS-Data-IdentitySchema-002` | 2026-08-11 | Identity Schema Execution | COMPLETE | Applied additive SQL identity schema migration (`sql/002_identity_foundation.sql`), registered physical terminal `3392113170057`, created `device_users` foundation, verified 100% attendance log preservation (6/6 records), and verified collector & healthcheck compatibility. |
| `ADMS-Collector-IdentityTransition-001` | 2026-08-11 | Identity Transition Plan + Git Hygiene | COMPLETE | Detailed design for Collector identity transition (`ensure_device_user`), identified legacy FK constraint blocker (`attendance_logs_user_id_fkey`), updated `.gitignore`, and untracked local AI rules/prompt history from Git index while preserving local files. |
| `ADMS-Data-LegacyIdentityConstraint-001` | 2026-08-11 | Constraint Transition Plan | COMPLETE | Detailed design for dropping `attendance_logs_user_id_fkey` constraint while preserving `user_id` string column and `UNIQUE (user_id, device_ip, scan_time)` constraint, unblocking Collector transition away from `ensure_employee_stub()`. |
| `ADMS-Data-LegacyIdentityConstraint-002` | 2026-08-11 | Constraint Transition Execution | COMPLETE | Applied SQL migration `sql/003_legacy_identity_constraint.sql` dropping `attendance_logs_user_id_fkey` constraint, preserved raw `user_id` column & `UNIQUE (user_id, device_ip, scan_time)` dedupe constraint, verified 100% attendance preservation (6/6 records), and unblocked Collector transition (`ADMS-Collector-IdentityTransition-002`). |
| `ADMS-Collector-IdentityTransition-002` | 2026-08-11 | Identity Transition Execution | COMPLETE | Implemented Collector database layer identity transition (`app/db.py`), removed `ensure_employee_stub()`, added `get_or_create_device()`, `ensure_device_user()`, and `resolve_verified_employee_mapping()`, added test suite (28/28 passed 100%), verified live against physical terminal `192.168.1.201` (0 new stubs created, unmapped scans stored cleanly with `employee_id = NULL`). |
| `ADMS-Checkpoint-PostIdentityTransition-001` | 2026-08-11 | Post-Identity Checkpoint | COMPLETE | Formal recovery checkpoint post-identity transition. Verified `human_employees` 0 records, verified 0 new legacy stubs created, generated real PostgreSQL custom-format backup archive (`adms_post_identity_20260811_113944.dump`), and verified archive listing via `pg_restore -l`. |
| `ADMS-Data-ExcelImport-001` | 2026-08-11 | Excel Import Plan | COMPLETE | Profiled 120 clean Human Master records across 4 categories (`นายทหาร` 20, `พันจ่า` 58, `จ่า` 6, `พลทหาร` 36), verified 0 duplicates, established mapping contract for `human_employees`, rejected 1..120 row mapping assumption. |
| `ADMS-Data-HumanMasterSchema-001` | 2026-08-11 | Schema Readiness Plan | COMPLETE | Reviewed `human_employees` schema, rejected invalid/unsafe `ON CONFLICT (display_name)` idempotency, designed additive migration `sql/004_human_master_schema.sql` (`branch`, `category`, `human_employee_sources` provenance table), selected Option B decision gate. |
| `ADMS-Data-HumanMasterSchema-002` | 2026-08-11 | Schema Foundation Execution | COMPLETE | Applied SQL migration `sql/004_human_master_schema.sql` adding `branch` & `category` columns to `human_employees`, created `human_employee_sources` provenance linkage table (`UNIQUE (source_system, source_record_key)`), created pre-migration backup (`adms_pre_schema004_20260811_115214.dump`), and verified unit test suite (28/28 passed). |
| `ADMS-Docs-Categorize-001` | 2026-08-11 | Docs Categorization Plan | COMPLETE | Inventoried 55 documentation files, classified 20 canonical root docs into 6 domain categories (`architecture/`, `device/`, `collector/`, `data/`, `database/`, `operations/`), mapped flat reports retention, designed `docs/README.md` navigation map. |
| `ADMS-Docs-Categorize-002` | 2026-08-11 | Docs Categorization Execution | COMPLETE | Reorganized 20 canonical docs into 6 domain subdirectories using `git mv`, separated AI-Brain docs under `docs/external/ai-brain/`, created top-level `docs/README.md` navigation map using relative Markdown links, updated project cross-references. |
| `ADMS-Data-ExcelImport-002` | 2026-08-11 | Excel Import Execution | COMPLETE | Implemented import utility `app/import_excel_human_master.py`, verified dry-run (120 records, 4 categories), generated pre-import recovery backup `adms_pre_excel_import_20260811_120503.dump`, added test suite (33/33 passed 100%), verified zero terminal access. |
| `ADMS-Checkpoint-PostExcelImport-001` | 2026-08-11 | Post-Import Checkpoint | COMPLETE | Verified 120 `human_employees` & 120 `human_employee_sources` records, 0 mappings created, 6/6 attendance logs preserved, generated post-import recovery backup `adms_post_excel_import_20260811_121449.dump`, verified archive via `pg_restore -l`. |
| `ADMS-Data-HumanDeviceMapping-001` | 2026-08-11 | Human Device Mapping Plan | COMPLETE | Designed Human ↔ Device mapping architecture, evidence hierarchy, controlled test-scan workflow, schema gap analysis. Identified missing auditability & temporal identity. Selected Route B: `ADMS-Data-HumanDeviceMappingSchema-001`. |
| `ADMS-Data-HumanDeviceMappingSchema-001` | 2026-08-11 | Mapping Schema Plan | COMPLETE | Designed DDL migration `sql/005_human_device_mapping_schema.sql` adding temporal bounds (`valid_from`, `valid_to`), audit fields (`verified_by`, `verification_method`, `verification_note`), active partial unique index, and rollback plan. |
| `ADMS-Data-HumanDeviceMappingSchema-002` | 2026-08-11 | Mapping Schema Execution | COMPLETE | Applied migration `sql/005_human_device_mapping_schema.sql` — temporal bounds, audit fields, `chk_temporal_validity`, `chk_verified_metadata`, `chk_verification_method`, partial unique index `idx_active_verified_device_user`. |
| `ADMS-Data-DeviceUserLifecycle-001` | 2026-08-11 | Lifecycle Audit | COMPLETE | Verified `ensure_device_user()` identity reuse, recycling risks, selected Decision B (roster lifecycle columns). |
| `ADMS-Checkpoint-PostMappingSchema-001` | 2026-08-11 | Post-Schema Checkpoint | COMPLETE | Resolved backup metadata conflict, verified 33/33 tests pass, locked next phase. |
| `ADMS-Server-DatabaseRebuild-001` | 2026-08-11 | Server Rebuild | COMPLETE | Reconstructed ADMS deployment: 3 Docker build fixes (package layout, iputils-ping, DB env vars), Collector fully operational (LIVE state, 7 attendance records backfilled from live terminal), 33 tests passed, authoritative backup created and verified. |
| `ADMS-Checkpoint-PostServerRebuild-001` | 2026-08-11 | Post-Rebuild Checkpoint | COMPLETE | READ-ONLY validation of reconstructed deployment. Verified git sync (TELEPHONE=origin=ai-brain=`a240061`), 3 ADMS containers running 0 restarts, DB row counts match (human_employees=120, attendance_logs=7, mappings=0), schema 005 constraints present, Collector LIVE+HEALTHY, ZKTeco connected, backup 44980 bytes SHA256 `5386681d...` verified 79 TOC entries, 33 tests passed. |
| `ADMS-Collector-TemporalIdentity-001` | 2026-08-11 | Temporal Identity Audit & Plan | COMPLETE | PLAN ONLY — Audited current timeless resolver (`resolve_verified_employee_mapping`), designed temporal `[valid_from, valid_to)` contract, traced realtime & backfill ingestion paths, identified TIMEZONE BLOCKER (pyzk naive Bangkok local UTC+7 stored as TIMESTAMPTZ interpreted as UTC → +7h offset), classified parse_time bug as NON-BLOCKING, verified Schema 005 & existing indexes sufficient, produced implementation plan for `ADMS-Collector-TemporalIdentity-002`. |
| `ADMS-Collector-TimestampTimezone-001` | 2026-08-11 | Timestamp Timezone Audit & Plan | COMPLETE | PLAN ONLY — Independently verified +7h offset on all 7 attendance rows (raw_payload + terminal data confirm), designed `normalize_device_timestamp()` with `ZoneInfo("Asia/Bangkok")` (Python 3.12 zoneinfo available), deterministic Strategy A correction (-7h UPDATE), 17 planned tests, identified latent watermark comparison bug (naive vs aware TypeError), confirmed no PostgreSQL/container TZ change needed. |
| `ADMS-Collector-TimestampTimezone-002` | 2026-08-11 | Timestamp Timezone Implementation | COMPLETE | Implemented `normalize_device_timestamp()` in `app/timestamp_utils.py`, updated `app/db.py` (realtime + backfill), `app/collector.py` (watermark comparison), `app/mqtt_client.py` (MQTT payload), corrected 7 historical rows -7h in single transaction, 21 new tests (54/54 total pass), rebuilt collector, runtime verified (0 duplicates, backfill idempotent, LIVE+HEALTHY), pre/post backups verified, all 3 nodes synced at `44202d4`. |
| `ADMS-Collector-TimestampTimezone-003` | 2026-08-11 | Timestamp Timezone Live Verification Checkpoint | COMPLETE | READ-ONLY checkpoint. Verified git sync (all 3 nodes at `5adae55`), runtime healthy (3 containers, collector LIVE), DB integrity (all counts match), 7/7 attendance rows round-trip PASS (0 remaining +7h errors), 0 duplicates (UNIQUE constraint intact), `normalize_device_timestamp()` present in all 4 code paths, 54/54 tests pass, pre/post backups verified (`pg_restore -l` PASS), ZKTeco connected (no writes), parse_time defect NON-BLOCKING, TemporalIdentity-002 UNBLOCKED. |
| `ADMS-Collector-TemporalIdentity-002` | 2026-08-11 | Temporal Identity Implementation | COMPLETE | Implemented temporal `resolve_verified_employee_mapping(cur, device_user_pk, scan_time)` with `[valid_from, valid_to)` semantics (valid_from inclusive, valid_to exclusive), VERIFIED-only filtering, LIMIT 2 ambiguity defense (multiple matches → None + error log), updated realtime path (`save_attendance_log`) and backfill path (`save_attendance_batch` — per-record resolution), 33 new tests (87/87 total pass), deployed to ai-brain, collector rebuilt, runtime LIVE+HEALTHY (0 restarts), 0 mappings, all 7 attendance rows employee_id NULL, Human Master 120 preserved, no device writes. |
| `ADMS-Collector-TemporalIdentity-003` | 2026-08-11 | Temporal Identity Live Verification Checkpoint | COMPLETE | READ-ONLY checkpoint PASS. Verified git sync (all 3 nodes at `ea80fb4`), resolver source verified on ai-brain (VERIFIED-only, `[valid_from, valid_to)`, LIMIT 2 ambiguity defense, canonical scan_time post-normalization), realtime + backfill paths verified, dedupe constraint unchanged (0 duplicates), timestamp regression PASS (0 +7h errors), runtime LIVE+HEALTHY (0 restarts), 87/87 tests pass, Human Master 120/120 preserved, backup verified (`pg_restore -l` PASS), 0 mappings, device unmodified. Human ↔ Device Mapping PLAN AUTHORIZED. |
| `ADMS-Data-HumanDeviceMapping-002` | 2026-08-11 | Human Device Mapping Workflow Plan | COMPLETE | READ-ONLY / WORKFLOW PLAN ONLY. Designed administrator-controlled mapping workflow, evidence hierarchy (6 tiers), controlled scan workflow (6 steps + batch variant), temporal `[valid_from, valid_to)` policy, audit metadata vocabulary (`verified_by`, `verification_method`, `verification_note`), overlap protection (partial unique index + temporal overlap detection), reconciliation design (post-mapping + unlink/correction), mapping creation transaction design, dry-run preview, positive resolution plan, device user recycling safety, roster lifecycle relationship, parse_time impact assessment. Candidate reports for device users 1 and 2: both have generic placeholder display names (`Device User 1`, `Device User 2`) — controlled scan is the ONLY viable verification path. 0 mappings created, 0 schema changes, 0 terminal writes. |
| `ADMS-Data-DeviceEnrollmentWorkflow-001` | 2026-08-12 | Device Enrollment Workflow Post-Reset Audit & Plan | COMPLETE | READ-ONLY LIVE AUDIT / PLAN ONLY + DOCUMENTATION WRITE ONLY. Triggered by owner manually deleting all terminal users from ZKTeco device and restarting system. Verified post-reset terminal state: 0 users (roster empty), 7 attendance records preserved in flash memory. DB baseline: human_employees=120, device_users=2 (STALE — active=true, inactive_at=NULL), employee_device_mappings=0, attendance_logs=7 (all unmapped). Confirmed lifecycle gap: automatic roster lifecycle detection NOT IMPLEMENTED. Designed production enrollment workflow: ID namespace 1001+ (legacy IDs 1, 2 retired as LEGACY_TEST_ID), 12-step controlled enrollment workflow, account state machine (AVAILABLE→RESERVED→CREATED→ENROLLED→CONFIRMED→VERIFIED_MAPPING), Option A (ADMS creates terminal account via `set_user()`), fingerprint enrollment at PHYSICAL TERMINAL only, temporal policy (valid_from=controlled scan timestamp, no backdating), pilot design (1 Human, 1 account, 1 controlled scan), bulk rollout (5-10 per batch), failure handling, biometric security boundary, proposed `enrollment_reservations` table (PLAN ONLY). 0 terminal writes, 0 mappings created, 0 schema changes, 0 application changes. |
| `ADMS-Data-DeviceUserLifecycle-002` | 2026-08-12 | Device User Lifecycle Implementation | COMPLETE | WRITE mode. Implemented `reconcile_roster_lifecycle()` in `app/db.py` (atomic per-device roster reconciliation: observed users → ensure + roster_last_seen_at + clear inactive_at, missing users → set inactive_at if NULL, UID change detection, no deletions). Added `perform_roster_lifecycle_check()` in `app/collector.py` (roster snapshot in BACKFILLING, periodic polling in LIVE every `roster_poll_interval_seconds` default 300s, failure-safe: exception/None → no lifecycle updates). Added `roster_poll_interval_seconds` to `app/config.py`. 18 new tests (105/105 total pass). Pre-write backup verified (45049 bytes, 79 TOC entries). Deployed to ai-brain (listener rebuilt only, PostgreSQL/MQTT untouched). First poll LIVE VERIFIED: 0 terminal users → both device_users (id=1, id=2) marked inactive_at=2026-08-12 04:31:54+00, active=false. Row counts unchanged (120/2/0/7, sync_events +1 ROSTER_LIFECYCLE). 0 deletions, 0 mappings, 0 human_employees created, 0 schema changes, 0 terminal writes. Post-write backup verified (45172 bytes). |
| `ADMS-Data-DeviceUserLifecycle-003` | 2026-08-12 | Device User Lifecycle Live Verification Checkpoint | COMPLETE (Latest Task) | READ-ONLY checkpoint PASS. Verified git sync (all 3 nodes at `91c7012`). Runtime: 3 containers healthy, 0 restarts, collector LIVE+HEALTHY. Roster lifecycle: 13+ periodic polls at 300s interval, ALL showing 0 marked_inactive after initial 2 at 04:31:54. `inactive_at` T1==T2 stability proven (`04:31:54.095625+00` for both users, 15+ minutes apart). DB baseline: human_employees=120, human_employee_sources=120, devices=1, device_users=2, employee_device_mappings=0, attendance_logs=7 (all employee_id NULL), 0 duplicates. Human Master integrity: 120 unique UUIDs, 0 orphans, 0 duplicate source keys. Source verified: `reconcile_roster_lifecycle()` (inactive_at IS NULL guard), `perform_roster_lifecycle_check()` (failure-safe, None check), `roster_poll_interval_seconds` (default 300). 105/105 tests pass. Post-lifecycle backup verified (45172 bytes, SHA256 `390832323c...`, 79 TOC entries, `pg_restore -l` PASS). 0 terminal writes, 0 DB exceptions, 0 UID anomalies, 0 enrollment. DeviceEnrollmentWorkflow-002 UNBLOCKED. |
| `ADMS-Data-DeviceEnrollmentWorkflow-002` | 2026-08-12 | Device Enrollment Workflow Implementation | COMPLETE | WRITE mode. Implemented `app/enrollment.py`: production ID allocator (1001+, legacy 1/2 never reused, per-device advisory-lock serialization, monotonic no-recycling), reservation (`reserve_next_device_user_id` — Human/device validation, operator capture, duplicate prevention), safe terminal account creation (`create_reserved_terminal_account` — set_user with NORMAL privilege=0, exact reserved ID, fail-safe on existing account/unreachable device), roster verification (`verify_terminal_account_created` — device_uid capture, privilege re-check), 9-state workflow (RESERVED→TERMINAL_ACCOUNT_CREATED→FINGERPRINT_ENROLLMENT_PENDING→FINGERPRINT_ENROLLED→CONTROLLED_SCAN_PENDING→CONTROLLED_SCAN_CONFIRMED→READY_FOR_MAPPING, +CANCELLED/RETIRED) enforced in code and DB, controlled-scan window (5 min default), display-name policy (ASCII-safe). Applied additive migration `sql/006_device_user_enrollment_schema.sql` (device_user_enrollments + 5 indexes incl. partial unique uq_active_enrollment_per_human_device + 5 constraints) with verified pre/post backups (pg_restore -l PASS). 63 new tests (168/168 pass). Deployed to ai-brain via pty-mcp (git pull, listener rebuilt only, PostgreSQL/MQTT untouched). Post-deploy: collector LIVE+HEALTHY (HC_RC=0, 0 restarts), DB baseline intact (120/120/1/2/7/0), device_user_enrollments=0, 0 terminal accounts created, 0 mappings, 0 schema damage, device unmodified. Pilot infrastructure READY. |
| `ADMS-Data-DeviceEnrollmentPilot-001` | 2026-08-12 | One-Human Controlled Pilot | COMPLETE (Latest Task) | WRITE mode — LIMITED DEVICE MUTATION. Owner confirmed pilot Human **กฤตพล หมาดเส็น** (พ.จ.ต., employee_id `039c4486-b30f-4ce1-b780-783cd268858d`). Reserved production ID **1001** via allocator (enrollment #1, RESERVED, reserved_by owner-krittaphol). Pre-write backup verified (`adms_pre_pilot_20260812_153220.dump`, 52,840 B, SHA256 `56ffb4ef…`, pg_restore -l PASS). Created exactly ONE terminal account: **1001 / `cpo3 Krittapon M` / NORMAL privilege (0) / device_uid 1** (pyzk set_user returned False but roster proved creation; owner-approved reconciliation via canonical module path → device_users pk 7, enrollment TERMINAL_ACCOUNT_CREATED). Legacy IDs 1/2 remain INACTIVE. Physical fingerprint enrollment by Human at terminal (FINGERPRINT_ENROLLED, confirmed_at 08:46:42+00). Controlled scan window 5 min (deadline 08:51:45+00) → ONE scan verified: attendance id 12, user 1001, device_user_pk 7, scan_time 2026-08-12 08:47:37+00, unambiguous, employee_id NULL. Owner confirmed identity (CONFIRM CONTROLLED SCAN). Enrollment #1 → CONTROLLED_SCAN_CONFIRMED → **READY_FOR_MAPPING** (recommended valid_from `2026-08-12 08:47:37+00`, source CONTROLLED_SCAN, confidence STRONG). Post-pilot backup verified (`adms_post_pilot_20260812_155112.dump`, 53,334 B, SHA256 `80aaed55…`, pg_restore -l PASS). DB: 120/120/1/3/8/0 (device_users 2 historical inactive + 1 production active; attendance 7 historical + 1 controlled scan). 0 mappings, 0 bulk enrollment, 0 remote fingerprint ops, 0 templates stored. 168/168 tests PASS. Runtime HEALTHY (HC_RC=0, 0 restarts, ZKTeco CONNECTED). HumanDeviceMapping-003 UNBLOCKED. |
| `ADMS-Data-HumanDeviceMapping-003` | 2026-08-12 | First VERIFIED Mapping + RTN Rank Normalization | COMPLETE (Latest Task) | WRITE mode — LIMITED PRODUCTION MAPPING + RANK RESEARCH. Pre-write backup verified (`adms_pre_mapping_20260812_183103.dump`, 53,742 B, SHA256 `7b04493a…`, pg_restore -l PASS). Created exactly ONE production VERIFIED temporal mapping via canonical `app/mapping.create_verified_mapping()`: mapping_id 1, employee `039c4486-b30f-4ce1-b780-783cd268858d` ↔ device_user_pk 7 (terminal 1001, NORMAL), valid_from `2026-08-12 08:47:37+00` (controlled scan boundary), valid_to NULL, method CONTROLLED_SCAN, verified_by owner-krittaphol, note references account 1001 / fingerprint enrollment / attendance id 12 / owner confirmation / pilot PromptID. Resolver verified LIVE: before valid_from → None; at/after → Human; legacy pks 1,2 → None; ambiguity fail-safe intact. attendance id 12 remains employee_id NULL (reconciliation deferred, out of scope). DB: 120/120/1/3/8/**1**/1. Implemented RTN rank normalization (`app/rtn_ranks.py` — 16-rank canonical catalog + acting forms; reference layer only, no schema change) and พลทหาร exclusion policy (`is_plothan()` + import boundary `--exclude-plothan`); audited **36 พลทหาร records** — retained, archival migration proposed. Deployed via pty-mcp (git pull ff-only, listener rebuilt, healthy, restarts 0). 43 new tests (211/211 PASS). Post-write backup verified (`adms_post_mapping_20260812_183254.dump`, 54,170 B, SHA256 `24fca8d3…`, pg_restore -l PASS). Runtime HEALTHY. HumanDeviceMapping-004 NEXT. |
| `ADMS-Data-HumanDeviceMapping-004` | 2026-08-12 | Post-Mapping Checkpoint + Owner Gate | COMPLETE (Latest Task) | CHECKPOINT PASS. Independently verified: git sync `04e9974`, runtime HEALTHY (restarts 0, HC_RC=0), DB 120/120/1/3/1/1/8, mapping id 1 (VERIFIED, valid_from 08:47:37+00), Human record unaltered, enrollment evidence chain consistent, temporal resolver (before→None/at→Human/after→Human/legacy→None), attendance id 12 intact (raw_payload unchanged, DUPES=0), RTN rank canonical values live, พลทหาร 36 (no deletion), tests 211/211, post-mapping backup verified (`24fca8d3…`). Owner selected C (Both, sequentially) at the SUBMIT gate → same-session sub-phases: AttendanceReconciliation-001 + PlothanProductionExclusion-001 (both PASS). |
| `ADMS-Data-AttendanceReconciliation-001` | 2026-08-12 | Deterministic Attendance Reconciliation | COMPLETE | WRITE sub-phase (owner gate C). Reconciled exactly attendance **id 12** → employee `039c4486-b30f-4ce1-b780-783cd268858d` (single-row transaction; scan_time == valid_from inside the sole VERIFIED interval; ambiguity count 1). Pre-backup (`adms_pre_reconcile_20260812_190956.dump`, 54,267 B, SHA256 `87103a73…`) + post-backup (`adms_post_reconcile_20260812_191849.dump`, 54,317 B, SHA256 `4ccde67c…`) verified. Post-verify: ATT_WITH_HUMAN=1, ATT_TOTAL=8, DUPES=0, LEGACY_ATTRIBUTED=0, raw_payload unchanged, resolver intact. 7 historical legacy rows remain NULL (before valid_from — never attributed). |
| `ADMS-Data-MultiFingerprintValidation-001` | 2026-08-12 | Same-Human Multi-Fingerprint Validation | COMPLETE (Latest Task) | LIVE VALIDATION + OWNER GATES. Owner physically enrolled a SECOND fingerprint under EXISTING User 1001 (no User 1002 created; roster re-read confirmed `1001/uid 1/privilege 0` only). Fingerprint slot count NOT SAFELY OBSERVABLE → behavioral validation. Two controlled second-finger scans (attendance id 15 @ 13:28:04+00, id 16 @ 13:30:47+00 — both inside the 5-min window) resolved AUTOMATICALLY to the SAME Human `039c4486-b30f-4ce1-b780-783cd268858d` via mapping_id 1 (REALTIME_RESOLUTION PASS, no manual UPDATE). Owner explicitly confirmed both scans were theirs with the new finger. Acceptance: mappings stayed exactly 1 (VERIFIED), device_user_pk 7 unchanged, valid_from `08:47:37+00` unchanged, valid_to NULL, DUPES=0, legacy 7 rows untouched (employee_id NULL), 36 พลทหาร still `production_scope=false`, pilot Human `true`, NON_PLOTHAN_FLAGGED=0. 0 new Humans / 0 new terminal users / 0 new mappings / 0 templates stored. 213/213 tests PASS. Runtime HEALTHY (restarts 0, HC_RC=0, ZKTeco CONNECTED). Post-validation backup verified (`adms_post_multifinger_20260812_133403.dump`, 55,013 B, SHA256 `69bd1be8…`, pg_restore -l 105 TOC PASS). Explicit limitation: validates SAME-HUMAN MULTI-FINGERPRINT only — does NOT validate multi-person enrollment. |
| `ADMS-Backend-Finalization-001` | 2026-08-12 | Backend/Data/Identity Finalization | COMPLETE (Latest Task) | AUDIT → OWNER GATE → FIX → HISTORICAL CORRECTION → TEST → DEPLOY → LIVE VERIFY → FINAL ACCEPTANCE. Owner gate: APPLY FIX (A). parse_time defect reproduced live (`parse_time('05:00:00')` → ValueError) — root cause `HH:MM`-only parser vs production `HH:MM:SS` window. Fixed `app/db.py parse_time()` to accept HH:MM + HH:MM:SS (invalid → ValueError → UNKNOWN fail-safe; timezone untouched). 11 new tests (`tests/test_parse_time.py`; 224/224 PASS). Deployed ai-brain (listener rebuilt only; restarts 0, HC_RC=0; fix verified live → ON_TIME). Historical correction: 10/10 UNKNOWN rows deterministically recomputed via deployed canonical logic (status-only, one transaction) → 8 LATE + 2 ON_TIME; scan_time/raw_payload/identity untouched; DUPES=0. Regressions PASS: temporal resolver (before→None/at→Human/after→Human/legacy→None), multi-fingerprint ids 15/16 → same Human, mappings=1, 36 พลทหาร excluded, NON_PLOTHAN_FLAGGED=0, pilot production_scope=true, RTN rank canonical values, is_plothan PASS. Backups verified: pre `adms_pre_finalization_20260812_140255.dump` + authoritative **`adms_backend_final_20260812_140826.dump`** (55,149 B, SHA256 `08169e66…`, 105 TOC). Backend completeness A–Z: all COMPLETE. Frontend READY (API gap: no HTTP layer). `docs/BACKEND_PRODUCTION_BASELINE.md` created. Backend Foundation **100% COMPLETE**; Frontend UNBLOCKED; Native ADMS Push EXPERIMENTAL/DEFERRED. |
| `ADMS-Data-PlothanProductionExclusion-001` | 2026-08-12 | พลทหาร Production Scope Exclusion | COMPLETE | WRITE sub-phase (owner gate C). Additive migration `sql/007_plothan_production_scope.sql`: `production_scope BOOLEAN NOT NULL DEFAULT true` + deterministic flip of the 36 พลทหาร records to `false` (transactional, idempotent, reversible — rollback UPDATE documented). NO DELETE; UUIDs/human_employee_sources/attendance/mapping preserved; NON_PLOTHAN_FLAGGED=0. Enforcement: `reserve_next_device_user_id()` requires `production_scope=true` (2 new tests; 213/213 PASS). Pre-backup (`5835be84…`) + post-backup (`adms_post_plothan_excl_20260812_192923.dump`, 54,798 B, SHA256 `fe8310d8…`, 105 TOC) verified. Listener rebuilt, HEALTHY, restarts 0, HC_RC=0. Pilot Human remains production_scope=true. |

---

## Pending & Upcoming Work

1. **Further Controlled Enrollment (owner-gated)** (see item 3).

2. ~~พลทหาร Archival / Exclusion Migration~~ — **DONE** (reversible `production_scope` flag, migration 007).

3. **Further Controlled Enrollment (owner-gated)**:
   - Next personnel enrollment can follow the proven pilot workflow (reservation → account → physical fingerprint → controlled scan → VERIFIED mapping); the allocator, state machine, rank normalization and `production_scope` enforcement are all in place. Bulk enrollment remains NOT AUTHORIZED.
   - Multi-fingerprint behavior for the SAME Human is now PROVEN (`ADMS-Data-MultiFingerprintValidation-001`): multiple physical fingers on ONE terminal account resolve through ONE VERIFIED mapping. Multi-person enrollment still NOT validated.

4. ~~Parse_Time Bug Fix~~ — **DONE** (`ADMS-Backend-Finalization-001`: HH:MM:SS supported; 10/10 historical rows corrected; 224/224 tests).

5. **Frontend Development (owner-gated — plan ready)**:
   - Backend foundation 100% COMPLETE; Frontend UNBLOCKED. `docs/FRONTEND_ARCHITECTURE_PLAN.md` prepared (stack proposal awaiting owner approval). First executable step: **API Gap Closure (F1)** — add a read-only FastAPI layer + OpenAPI before UI work; then core read UI (F2), enrollment workflow UI (F3), admin/mapping views (F4), auth hardening (F5).

6. **Further Controlled Enrollment (owner-gated)**:
   - Next personnel enrollment can follow the proven pilot workflow; allocator, state machine, rank normalization, production_scope enforcement and multi-fingerprint behavior are all in place. Multi-person physical validation DEFERRED until personnel available.

7. **Native ADMS Push E2E** (Locked Step):
   - Experimental track only. Deferred until identity workflow foundation is complete.
